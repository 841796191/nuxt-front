<template>
  <div class="container">index</div>
</template>

<script>
export default {
  // async asyncData({ $axios }) {
  // context参数包含所有模块的对象,可从里面取axios、store等
  // ErrorHandle($axios, redirect)
  // const result = await $axios.$get('http://localhost:8089/posts')
  // return {
  // 使用$axios.get,则返回的数据在result.data中
  // 使用$axios.$get(),则返回的数据在result中
  // posts: result,
  // }
  // },
}
</script>

<style></style>
